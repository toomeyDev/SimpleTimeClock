Available input formats include: 
 -> separate_prompt (Prompt for hours, minutes, and seconds separately)
 -> condensed_prompt (Prompt for hours, minutes, and seconds in one-line)
 -> simplified_prompt (prompt for just hours and minutes, with a simple format)
	
To choose one of these formats, type 'entry-format-set' at the menu,
then choose the format you prefer.
